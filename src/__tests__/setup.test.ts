/**
 * Arquivo de setup de testes
 * Este arquivo apenas configura o ambiente de testes
 */

describe('Test Setup', () => {
  it('deve configurar ambiente de testes', () => {
    // Este teste apenas garante que o setup está funcionando
    expect(true).toBe(true);
  });
});

