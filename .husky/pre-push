#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🚀 Executando validações pré-push (não bloqueantes)..."

# Executar testes unitários (não bloqueante)
echo "🧪 Executando testes unitários..."
npm run test:unit || echo "⚠️ Testes unitários falharam, mas não bloqueando push."

# Executar testes de integração (não bloqueante)
echo "🔗 Executando testes de integração..."
npm run test:integration || echo "⚠️ Testes de integração falharam, mas não bloqueando push."

echo "✅ Validações pré-push concluídas (testes podem ter falhado, mas push continuará)."
