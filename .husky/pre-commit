#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Executando lint-staged (formatação automática)..."

# Executar lint-staged para formatar e validar arquivos staged
npx lint-staged

if [ $? -ne 0 ]; then
  echo "❌ Lint-staged falhou!"
  echo "🚨 COMMIT BLOQUEADO - Corrija os problemas antes de continuar"
  exit 1
fi

echo "✅ Formatação automática concluída!"
echo "🎉 Commit autorizado!"
